# HEPHAITOS 코드베이스 딥다이브 분석

> **생성일**: 2025-12-14
> **버전**: v1.0
> **분석 범위**: 전체 프로젝트 아키텍처, 핵심 모듈, 기술 부채

---

## 1. 프로젝트 개요

### 1.1 비전
**"Replit for Trading"** - Copy-Learn-Build 패러다임의 투자 교육 플랫폼

### 1.2 핵심 기능 (3가지 축)
| 기능 | 설명 | 구현 상태 |
|------|------|----------|
| **COPY** | 셀럽 포트폴리오 따라하기 | ✅ 구현 완료 |
| **LEARN** | AI + 멘토 코칭 | 🔄 부분 구현 |
| **BUILD** | 자연어 AI 전략 빌더 | ✅ 구현 완료 |

---

## 2. 아키텍처 분석

### 2.1 기술 스택

```
Frontend:
├── Next.js 14+ (App Router)
├── TypeScript (Strict Mode)
├── Tailwind CSS + Custom Design Tokens
├── Zustand (상태 관리)
├── ReactFlow (전략 빌더)
└── Framer Motion (애니메이션)

Backend:
├── Next.js API Routes
├── Supabase (Auth + DB)
├── Redis (캐싱 + Rate Limiting)
└── External APIs (KIS, Polygon, Toss)

AI/ML:
├── Claude API (자연어 처리)
├── Intent Parser (의도 파싱)
└── Strategy Generator (전략 생성)
```

### 2.2 디렉토리 구조

```
src/
├── app/                    # Next.js App Router
│   ├── api/               # API Routes (27개)
│   │   ├── ai/           # AI 관련 (agent, report, strategy)
│   │   ├── broker/       # 브로커 연동
│   │   ├── celebrities/  # 셀럽 포트폴리오
│   │   ├── coaching/     # 코칭 기능
│   │   ├── exchange/     # 거래소 API
│   │   ├── payments/     # 결제 (Toss)
│   │   ├── portfolio/    # 포트폴리오
│   │   ├── simulation/   # 시뮬레이션
│   │   ├── strategies/   # 전략 CRUD
│   │   └── trades/       # 거래
│   ├── auth/             # 인증 페이지
│   ├── dashboard/        # 대시보드 페이지 (10개)
│   └── onboarding/       # 온보딩
│
├── components/            # React 컴포넌트 (~85개)
│   ├── ui/              # 기본 UI (20+)
│   ├── dashboard/       # 대시보드 위젯
│   ├── strategy-builder/ # 전략 빌더 노드
│   ├── backtest/        # 백테스트 UI
│   ├── charts/          # 차트 컴포넌트
│   └── landing/         # 랜딩 페이지
│
├── lib/                   # 핵심 비즈니스 로직
│   ├── agent/           # AI 트레이딩 에이전트
│   ├── ai/              # Claude 클라이언트
│   ├── api/             # API 프로바이더
│   ├── backtest/        # 백테스트 엔진
│   ├── broker/          # 브로커 어댑터
│   ├── coaching/        # 실시간 코칭
│   ├── config/          # 환경변수 검증 (Zod)
│   ├── data/            # 종목 마스터 캐시
│   ├── exchange/        # 거래소 연동
│   ├── mirroring/       # 셀럽 미러링
│   ├── monitoring/      # Sentry 모니터링
│   ├── payments/        # 결제 처리
│   ├── redis/           # Redis 캐시/Rate Limiter
│   ├── simulation/      # 시뮬레이션 계정
│   ├── trading/         # 트레이드 실행기
│   └── websocket/       # 실시간 데이터
│
├── stores/               # Zustand 스토어
│   ├── portfolio-store.ts
│   ├── strategy-store.ts
│   ├── exchange-store.ts
│   └── ui-store.ts
│
├── hooks/                # 커스텀 훅 (10+)
│   ├── use-backtest.ts
│   ├── use-websocket.ts
│   └── use-strategy-persistence.ts
│
├── types/                # TypeScript 타입 정의
│   └── index.ts         # 중앙 타입 정의
│
└── __tests__/           # 테스트 코드
    ├── api/
    ├── hooks/
    └── lib/
```

---

## 3. 핵심 모듈 분석

### 3.1 AI Trading Agent (`lib/agent/`)

**아키텍처**: Natural Language → Intent → Strategy → Execution

```
┌─────────────────────────────────────────────────────────────┐
│                    TradingAgentOrchestrator                 │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │ IntentParser│───▶│StrategyBuilder│───▶│  Executor  │    │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
│         │                 │                   │              │
│    Parse Intent     Build Strategy      Execute Trade       │
│         │                 │                   │              │
│    ┌────▼────┐      ┌────▼────┐        ┌────▼────┐         │
│    │ 10 Types│      │Validation│        │Risk Mgmt│         │
│    └─────────┘      └─────────┘        └─────────┘         │
└─────────────────────────────────────────────────────────────┘
```

**지원 Intent Types**:
- `STRATEGY_CREATE`: 전략 생성
- `STRATEGY_MODIFY`: 전략 수정
- `BACKTEST_RUN`: 백테스트 실행
- `LIVE_START/STOP`: 실거래 시작/중지
- `POSITION_QUERY`: 포지션 조회
- `MARKET_ANALYSIS`: 시장 분석
- `RISK_ADJUST`: 리스크 조정
- `HELP`: 도움말

**강점**:
- 한국어 자연어 처리 지원
- Conversation Context 유지
- 확인 필요 작업에 대한 confirmation flow

### 3.2 Backtest Engine (`lib/backtest/`)

**핵심 클래스**: `BacktestEngine`

```typescript
class BacktestEngine {
  // 핵심 메서드
  setData(data: OHLCV[]): void
  run(): Promise<BacktestResult>

  // 내부 로직
  simulate(): Promise<void>
  evaluateStrategy(candle: OHLCV, index: number): Signal
  processSignal(signal: Signal, candle: OHLCV): void
  checkRiskLimits(candle: OHLCV): void
  calculateMetrics(): BacktestMetrics
}
```

**계산 지표**:
| 지표 | 설명 |
|------|------|
| Sharpe Ratio | 위험 조정 수익률 |
| Sortino Ratio | 하방 위험 조정 수익률 |
| Calmar Ratio | MDD 대비 연간 수익률 |
| Max Drawdown | 최대 낙폭 |
| Win Rate | 승률 |
| Profit Factor | 총이익 / 총손실 |
| Expectancy | 기대값 |

### 3.3 Trade Executor (`lib/trading/`)

**Paper Trading vs Live Trading**:
```typescript
interface ExecutorConfig {
  enableLive: boolean      // 실거래 활성화 플래그
  paperTrading?: boolean   // 페이퍼 트레이딩 모드
  maxPositionSize: number  // 최대 포지션 크기 (%)
  riskConfig?: RiskConfig  // 리스크 관리 설정
}
```

**안전장치**:
- `enableLive = false`가 기본값
- 실거래 시 콘솔 경고 출력
- Stop Loss / Take Profit 자동 실행
- Emergency Close 기능

### 3.4 Celebrity Portfolio Mirroring (`lib/mirroring/`)

**지원 셀럽**:
| 이름 | 데이터 소스 | 업데이트 빈도 |
|------|------------|--------------|
| Nancy Pelosi | Congress Disclosure | 45일 지연 |
| Warren Buffett | SEC 13F | 분기별 |
| Michael Burry | SEC 13F | 분기별 |
| Cathie Wood | Public Filing | 실시간 |

**미러링 기능**:
- 포트폴리오 비중 복제
- 거래 알림
- AI 거래 분석

### 3.5 Strategy Builder (`components/strategy-builder/`)

**Visual Node-based Builder** (ReactFlow 기반)

**노드 타입**:
```
┌─────────┐   ┌──────────┐   ┌──────────┐
│ Trigger │──▶│ Condition│──▶│  Action  │
└─────────┘   └──────────┘   └──────────┘
      │              │
      │       ┌──────▼─────┐
      │       │  Indicator │
      │       └────────────┘
      │
┌─────▼─────┐
│   Risk    │
└───────────┘
```

**기능**:
- Undo/Redo 지원
- 전략 Import/Export (JSON)
- AI 전략 생성 통합
- 실시간 유효성 검사

---

## 4. API 구조 분석

### 4.1 API Routes 목록

| 경로 | 메서드 | 설명 |
|------|--------|------|
| `/api/ai/agent` | POST, PUT, DELETE | AI 에이전트 대화 |
| `/api/ai/report` | POST | AI 리포트 생성 |
| `/api/ai/entry-point` | POST | 진입점 분석 |
| `/api/broker` | GET, POST | 브로커 연동 |
| `/api/celebrities/trades` | GET | 셀럽 거래 내역 |
| `/api/celebrities/mirror` | POST | 포트폴리오 미러링 |
| `/api/exchange/markets` | GET | 마켓 목록 |
| `/api/exchange/tickers` | GET | 시세 조회 |
| `/api/exchange/ohlcv` | GET | OHLCV 데이터 |
| `/api/exchange/orderbook` | GET | 호가 데이터 |
| `/api/payments/webhook` | POST | Toss 웹훅 |
| `/api/portfolio` | GET | 포트폴리오 조회 |
| `/api/simulation` | GET, POST | 시뮬레이션 |
| `/api/simulation/trade` | POST | 시뮬레이션 거래 |
| `/api/strategies` | GET, POST | 전략 CRUD |
| `/api/strategies/[id]` | GET, PUT, DELETE | 전략 상세 |
| `/api/trades` | GET, POST | 거래 내역 |

### 4.2 인증/인가

- **Supabase Auth** 사용
- 미들웨어에서 세션 검증
- API Routes에서 `createClient(cookies())` 패턴

---

## 5. 기술 부채 및 개선점

### 5.1 Critical (즉시 해결 필요)

| 이슈 | 위치 | 설명 | 권장 해결책 |
|------|------|------|------------|
| **메모리 기반 Agent 세션** | `api/ai/agent/route.ts:12` | Map으로 세션 저장 → 서버 재시작 시 손실 | Redis 세션 스토어 |
| **백테스트 전략 파싱 미구현** | `backtest/engine.ts:162` | 하드코딩된 SMA 크로스오버 | strategy-parser.ts 연동 |
| **TODO: Claude API 연동** | `mirroring/celebrity-portfolio.ts:365` | Mock 분석 반환 | Claude API 호출 구현 |

### 5.2 High (조기 해결 권장)

| 이슈 | 위치 | 설명 | 권장 해결책 |
|------|------|------|------------|
| **Rate Limiter 미적용** | 대부분의 API 라우트 | Rate Limiter 구현됨, 미적용 | 미들웨어에서 적용 |
| **에러 핸들링 불일치** | API 라우트 전체 | 표준화된 에러 응답 필요 | ErrorHandler 일괄 적용 |
| **타입 정의 중복** | `types/` vs `*/types.ts` | 타입 분산 정의 | 중앙 집중화 |
| **Mock 데이터 하드코딩** | `celebrity-portfolio.ts` | 실제 API 연동 필요 | SEC API, Congress API 연동 |

### 5.3 Medium (계획적 해결)

| 이슈 | 위치 | 설명 | 권장 해결책 |
|------|------|------|------------|
| **테스트 커버리지 부족** | `__tests__/` | 주요 모듈 테스트 미비 | 핵심 비즈니스 로직 테스트 |
| **WebSocket 연결 관리** | `lib/websocket/` | 재연결 로직 미흡 | 지수 백오프 재연결 |
| **로깅 표준화** | 전체 | console.log 혼용 | safeLogger 전면 적용 |
| **환경변수 미검증** | 일부 모듈 | env.ts 검증 미사용 | 전면 적용 |

### 5.4 Low (장기 개선)

| 이슈 | 설명 | 권장 해결책 |
|------|------|------------|
| 번들 사이즈 최적화 | 대규모 라이브러리 포함 | Dynamic Import, Tree Shaking |
| 접근성 개선 | ARIA 속성 일부 누락 | 접근성 감사 수행 |
| 국제화 | 한국어 하드코딩 | i18n 적용 |
| 성능 모니터링 | 클라이언트 성능 추적 없음 | Web Vitals 수집 |

---

## 6. 개선 로드맵

### Phase 4: 안정화 (1-2주)

```
[ ] Rate Limiter 미들웨어 적용
[ ] 에러 핸들링 표준화
[ ] 로깅 표준화 (safeLogger)
[ ] Redis 세션 스토어 구현
```

### Phase 5: 기능 완성 (2-3주)

```
[ ] 백테스트 전략 파서 연동
[ ] Celebrity API 실제 연동
[ ] Claude 거래 분석 구현
[ ] WebSocket 재연결 로직
```

### Phase 6: 품질 향상 (3-4주)

```
[ ] 테스트 커버리지 70% 달성
[ ] E2E 테스트 확장
[ ] 성능 최적화
[ ] 접근성 개선
```

---

## 7. 아키텍처 다이어그램

### 7.1 전체 시스템 아키텍처

```
                           ┌─────────────────────────────────────────────────┐
                           │                   Frontend                       │
                           │  ┌─────────┐  ┌──────────┐  ┌───────────────┐   │
                           │  │ Landing │  │Dashboard │  │Strategy Builder│  │
                           │  └────┬────┘  └────┬─────┘  └───────┬───────┘   │
                           │       └───────────┼────────────────┘            │
                           │                   │                              │
                           └───────────────────┼──────────────────────────────┘
                                              │
                                    ┌─────────▼─────────┐
                                    │   Next.js API     │
                                    │   (API Routes)    │
                                    └─────────┬─────────┘
                                              │
              ┌───────────────────────────────┼───────────────────────────────┐
              │                               │                               │
    ┌─────────▼─────────┐        ┌───────────▼───────────┐       ┌──────────▼──────────┐
    │   AI Services     │        │   Trading Services    │       │  Data Services      │
    │ ┌───────────────┐ │        │ ┌─────────────────┐   │       │ ┌─────────────────┐ │
    │ │ Claude API    │ │        │ │ BacktestEngine  │   │       │ │ Stock Master    │ │
    │ │ IntentParser  │ │        │ │ TradeExecutor   │   │       │ │ Market Data     │ │
    │ │ StrategyBuilder│ │       │ │ Simulation      │   │       │ │ Celebrity Data  │ │
    │ └───────────────┘ │        │ └─────────────────┘   │       │ └─────────────────┘ │
    └─────────┬─────────┘        └───────────┬───────────┘       └──────────┬──────────┘
              │                              │                              │
              └──────────────────────────────┼──────────────────────────────┘
                                             │
                       ┌─────────────────────┼─────────────────────┐
                       │                     │                     │
             ┌─────────▼─────────┐ ┌─────────▼─────────┐ ┌────────▼────────┐
             │     Supabase      │ │      Redis        │ │  External APIs  │
             │ ┌───────────────┐ │ │ ┌───────────────┐ │ │ ┌─────────────┐ │
             │ │ Auth          │ │ │ │ Cache         │ │ │ │ KIS API     │ │
             │ │ Database      │ │ │ │ Rate Limiter  │ │ │ │ Polygon     │ │
             │ │ Storage       │ │ │ │ Token Cache   │ │ │ │ Toss        │ │
             │ └───────────────┘ │ │ └───────────────┘ │ │ │ Binance     │ │
             └───────────────────┘ └───────────────────┘ │ └─────────────┘ │
                                                         └─────────────────┘
```

### 7.2 데이터 흐름

```
User Input
    │
    ▼
┌─────────────────┐
│ Natural Language│
│   "비트코인 RSI  │
│   30이면 매수"   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│  Intent Parser  │────▶│ Parsed Intent   │
│                 │     │ type: CREATE    │
│                 │     │ symbol: BTC     │
│                 │     │ indicator: RSI  │
│                 │     │ condition: < 30 │
└─────────────────┘     └────────┬────────┘
                                 │
                                 ▼
                       ┌─────────────────┐
                       │Strategy Builder │
                       └────────┬────────┘
                                │
         ┌──────────────────────┼──────────────────────┐
         │                      │                      │
         ▼                      ▼                      ▼
┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
│   Backtest      │   │   Simulation    │   │  Live Trading   │
│   Engine        │   │   Account       │   │   Executor      │
└─────────────────┘   └─────────────────┘   └─────────────────┘
```

---

## 8. 결론

### 8.1 강점

1. **견고한 아키텍처**: 모듈화된 구조로 확장성 우수
2. **AI 통합**: 자연어 기반 전략 생성 구현
3. **안전 장치**: Paper Trading, Risk Management 구현
4. **타입 안정성**: TypeScript Strict Mode, Branded Types 사용
5. **Phase 1-3 완료**: 환경변수 검증, Redis 캐싱, 모니터링 구현

### 8.2 개선 필요 영역

1. **실제 API 연동**: Mock 데이터 → 실제 데이터
2. **테스트 커버리지**: 핵심 로직 테스트 확대
3. **Rate Limiting 적용**: 구현됨, 적용 필요
4. **세션 관리**: 메모리 → Redis 이관

### 8.3 다음 단계

1. Phase 4 안정화 작업 진행
2. 실제 API 연동 테스트
3. 프로덕션 배포 준비
4. 사용자 테스트 및 피드백 수집

---

*이 문서는 HEPHAITOS 코드베이스의 현재 상태를 분석한 결과입니다.*
*최종 업데이트: 2025-12-14*
