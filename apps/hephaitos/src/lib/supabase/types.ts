/**
 * Supabase Database Types
 *
 * Re-exports all types from the main database types file
 * to ensure consistency across the application.
 */

// Re-export everything from the main database types
export * from '@/types/database'
