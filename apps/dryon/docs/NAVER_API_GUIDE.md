# 네이버 API 설정 가이드

## 개요

네이버 검색 API를 사용하여 실시간으로 창업지원사업 공고를 수집합니다.

---

## 1. 네이버 개발자 센터 가입

### 1.1 회원가입
1. [네이버 개발자 센터](https://developers.naver.com/) 접속
2. 네이버 계정으로 로그인
3. 개발자 등록 (이름, 연락처 입력)

---

## 2. 애플리케이션 등록

### 2.1 애플리케이션 등록
1. 상단 메뉴 **"Application"** → **"애플리케이션 등록"** 클릭
2. 애플리케이션 정보 입력:

```
애플리케이션 이름: Hyein Agent
사용 API: 검색 (필수)
           ✅ 뉴스 검색
           ✅ 블로그 검색
비로그인 오픈 API 서비스 환경:
           ✅ WEB 설정 (필수)
```

### 2.2 서비스 환경 설정

#### ⚠️ 중요: 비로그인 오픈 API 서비스 환경 설정

**WEB 설정 (필수):**
```
웹 서비스 URL: http://localhost:3000
             (또는 실제 서비스 URL)
```

**설정 방법:**
1. "애플리케이션 등록" 화면에서 스크롤
2. **"비로그인 오픈 API 서비스 환경"** 섹션 찾기
3. **"WEB 설정"** 클릭
4. **"서비스 URL"** 입력란에 입력:
   - 로컬 개발: `http://localhost:3000`
   - 프로덕션: `https://yourdomain.com`

**예시:**
```
서비스 환경 추가
┌─────────────────────────────────┐
│ ▶ WEB 설정                      │
│   웹 서비스 URL                  │
│   http://localhost:3000         │
│   [추가]                         │
└─────────────────────────────────┘
```

### 2.3 등록 완료
- **"등록하기"** 버튼 클릭
- Client ID와 Client Secret 발급 확인

---

## 3. API 키 확인 및 복사

### 3.1 Client ID & Secret 확인
1. [내 애플리케이션](https://developers.naver.com/apps/#/list) 이동
2. 등록한 애플리케이션 클릭
3. **Client ID** 복사
4. **Client Secret** 복사

**형식:**
```
Client ID: AbCdEfGhIjKlMnOpQr
Client Secret: XyZ123456
```

---

## 4. 프로젝트 설정

### 4.1 .env 파일 설정

프로젝트 루트의 `.env` 파일에 추가:

```env
# ==================================
# 네이버 API
# ==================================
# 네이버 검색 API - https://developers.naver.com/docs/serviceapi/search/news/news.md
NAVER_CLIENT_ID=AbCdEfGhIjKlMnOpQr
NAVER_CLIENT_SECRET=XyZ123456
```

### 4.2 설정 확인

```bash
# 환경 변수 확인
echo $NAVER_CLIENT_ID
echo $NAVER_CLIENT_SECRET
```

---

## 5. 서비스 환경 추가 상세 가이드

### 5.1 왜 필요한가?
네이버 비로그인 오픈 API는 서비스 URL을 등록해야 사용할 수 있습니다.
- 보안을 위해 등록된 도메인에서만 API 호출 가능
- 개발/프로덕션 환경 각각 등록 가능

### 5.2 개발 환경 설정
```
WEB 설정
└─ http://localhost:3000
   http://localhost:8080
   http://127.0.0.1:3000
```

### 5.3 프로덕션 환경 설정
```
WEB 설정
└─ https://yourdomain.com
   https://www.yourdomain.com
   https://api.yourdomain.com
```

### 5.4 Docker 환경
```
WEB 설정
└─ http://localhost:3000
   http://0.0.0.0:3000
```

### 5.5 여러 환경 동시 등록
한 애플리케이션에 여러 URL 등록 가능:
```
✅ http://localhost:3000      (로컬 개발)
✅ http://localhost:8080      (Docker)
✅ https://dev.yourdomain.com (개발 서버)
✅ https://yourdomain.com     (프로덕션)
```

---

## 6. API 사용량 및 제한

### 6.1 일일 호출 한도
```
검색 API (뉴스/블로그):
- 기본: 25,000건/일
- 추가 신청 가능 (무료)
```

### 6.2 Rate Limiting
```
초당 호출 제한:
- 기본: 10 requests/sec
- 추가 신청 가능
```

### 6.3 추가 한도 신청
1. 애플리케이션 상세 페이지
2. "API 호출량 증가 신청"
3. 사유 작성 후 제출

---

## 7. 테스트

### 7.1 API 키 테스트

네이버 API 테스트 페이지:
```
https://developers.naver.com/docs/serviceapi/search/news/news.md
```

### 7.2 프로젝트에서 테스트

```bash
# 프로젝트 실행
npm start

# 로그 확인
# ✅ 네이버 검색 API: XX건 수집
```

### 7.3 수동 테스트 (curl)

```bash
# 뉴스 검색 테스트
curl -X GET "https://openapi.naver.com/v1/search/news.json?query=창업지원사업&display=5" \
  -H "X-Naver-Client-Id: YOUR_CLIENT_ID" \
  -H "X-Naver-Client-Secret: YOUR_CLIENT_SECRET"

# 블로그 검색 테스트
curl -X GET "https://openapi.naver.com/v1/search/blog.json?query=창업지원&display=5" \
  -H "X-Naver-Client-Id: YOUR_CLIENT_ID" \
  -H "X-Naver-Client-Secret: YOUR_CLIENT_SECRET"
```

---

## 8. 문제 해결

### 8.1 "서비스 환경 미등록" 오류

**오류 메시지:**
```json
{
  "errorMessage": "서비스 환경이 등록되지 않았습니다.",
  "errorCode": "024"
}
```

**해결 방법:**
1. [내 애플리케이션](https://developers.naver.com/apps/#/list) 접속
2. 해당 애플리케이션 클릭
3. **"API 설정"** 탭 → **"WEB 설정"** 확인
4. 서비스 URL 추가: `http://localhost:3000`

### 8.2 "인증 실패" 오류

**오류 메시지:**
```json
{
  "errorMessage": "인증에 실패했습니다.",
  "errorCode": "024"
}
```

**해결 방법:**
1. `.env` 파일의 `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET` 확인
2. 공백이나 따옴표 없이 입력했는지 확인
3. 애플리케이션 상태 확인 (활성화 여부)

### 8.3 "호출량 초과" 오류

**오류 메시지:**
```json
{
  "errorMessage": "호출량이 초과되었습니다.",
  "errorCode": "024"
}
```

**해결 방법:**
1. 일일 호출량 확인 (25,000건/일)
2. API 호출량 증가 신청
3. 캐싱 구현 (중복 호출 방지)

### 8.4 서비스 환경 추가가 안 보일 때

**증상:**
"비로그인 오픈 API 서비스 환경" 섹션이 보이지 않음

**해결 방법:**
1. 애플리케이션 수정 페이지로 이동
2. "사용 API" 재확인:
   - ✅ 검색 API 체크
   - ✅ 뉴스 검색 체크
   - ✅ 블로그 검색 체크
3. 저장 후 페이지 새로고침
4. 다시 "서비스 환경" 섹션 확인

---

## 9. 보안 주의사항

### 9.1 API 키 관리
```bash
# ❌ 절대 금지
git add .env
git commit -m "Add API keys"

# ✅ 올바른 방법
# .gitignore에 .env 추가
echo ".env" >> .gitignore
```

### 9.2 환경 변수 보안
```bash
# 프로덕션: 환경 변수로 직접 설정
export NAVER_CLIENT_ID=xxx
export NAVER_CLIENT_SECRET=xxx

# Docker: secrets 사용
docker secret create naver_client_id client_id.txt
docker secret create naver_client_secret client_secret.txt
```

### 9.3 코드 리뷰
- API 키가 코드에 하드코딩되지 않았는지 확인
- 로그에 API 키가 노출되지 않는지 확인

---

## 10. 추가 리소스

### 10.1 공식 문서
- [네이버 개발자 센터](https://developers.naver.com/)
- [검색 API 문서](https://developers.naver.com/docs/serviceapi/search/news/news.md)
- [뉴스 검색](https://developers.naver.com/docs/serviceapi/search/news/news.md)
- [블로그 검색](https://developers.naver.com/docs/serviceapi/search/blog/blog.md)

### 10.2 API 테스트 도구
- [네이버 API 콘솔](https://developers.naver.com/apps/#/list)
- [Postman Collection](https://www.postman.com/)

### 10.3 고객 지원
- [개발자 포럼](https://developers.naver.com/forum)
- [FAQ](https://developers.naver.com/docs/common/openapiguide/)

---

## 11. 체크리스트

설정 완료 여부를 확인하세요:

- [ ] 네이버 개발자 센터 가입
- [ ] 애플리케이션 등록
- [ ] 검색 API 선택 (뉴스 + 블로그)
- [ ] **비로그인 오픈 API 서비스 환경 - WEB 설정 추가** ⭐
- [ ] 서비스 URL 등록 (http://localhost:3000)
- [ ] Client ID 복사
- [ ] Client Secret 복사
- [ ] .env 파일에 추가
- [ ] API 테스트 성공
- [ ] 프로젝트 실행 확인

---

## 12. 빠른 설정 (요약)

```bash
# 1. 네이버 개발자 센터
https://developers.naver.com/apps/#/register

# 2. 애플리케이션 등록
이름: Hyein Agent
API: 검색 (뉴스 + 블로그)
WEB 설정: http://localhost:3000  ⭐ 중요!

# 3. .env 파일
NAVER_CLIENT_ID=발급받은_ID
NAVER_CLIENT_SECRET=발급받은_SECRET

# 4. 실행
npm start

# 5. 확인
✅ 네이버 검색 API: XX건 수집
```

---

**설정 완료!** 🎉

네이버 검색 API로 실시간 창업지원사업 공고를 수집할 수 있습니다!
