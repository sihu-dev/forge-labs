# HEPHAITOS 종합 아키텍처 문서

> **버전**: 1.0.0
> **업데이트**: 2025-12-19
> **목적**: 사업 UX + 엔진 + 기술 스택 통합 레퍼런스

---

## 1. 시스템 개요

### 1.1 비즈니스 모델: B2B2C

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    HEPHAITOS B2B2C 플랫폼 구조                           │
└─────────────────────────────────────────────────────────────────────────┘

                      HEPHAITOS (플랫폼)
                            │
            ┌───────────────┼───────────────┐
            │               │               │
            ▼               ▼               ▼
       ┌─────────┐    ┌─────────┐    ┌─────────┐
       │ 유튜버A │    │ 유튜버B │    │ 유튜버C │
       │ 10만 구독│    │ 50만 구독│    │ 5만 구독 │
       └────┬────┘    └────┬────┘    └────┬────┘
            │               │               │
            ▼               ▼               ▼
       ┌─────────┐    ┌─────────┐    ┌─────────┐
       │ 수강생  │    │ 수강생  │    │ 수강생  │
       └────┬────┘    └────┬────┘    └────┬────┘
            │               │               │
            └───────────────┼───────────────┘
                            │
                            ▼
                   ┌─────────────────┐
                   │  나만의 AI      │
                   │  트레이딩 에이전트│
                   └─────────────────┘

  수익 분배:
  ├── 수강료: 유튜버 70% / 플랫폼 30%
  ├── 크레딧: 유튜버 30% / 플랫폼 70%
  └── 브로커 CPA: 유튜버 50% / 플랫폼 50%
```

### 1.2 법적 포지셔닝

| 우리가 아닌 것 | 우리가 맞는 것 |
|--------------|--------------|
| 투자 자문업자 | 교육 콘텐츠 플랫폼 |
| 투자 일임업자 | 소프트웨어 도구 제공자 |
| 자산운용사 | 커뮤니티 플랫폼 |
| 로보어드바이저 | - |

**핵심 원칙**: "우리는 전략을 추천하지 않는다. 사용자가 스스로 학습하고 만드는 것을 돕는다."

---

## 2. 엔진 아키텍처

### 2.1 에이전트 시스템

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        HEPHAITOS Agent System                            │
└─────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────┐
  │  BacktestAgent     │  apps/hephaitos/src/agents/backtest-agent.ts
  │  (645 lines)       │
  ├────────────────────┤
  │  워크플로우:        │
  │  Initialize        │─┐
  │      ↓             │ │
  │  Simulate          │ │ Candle iteration
  │      ↓             │ │ Signal detection
  │  Calculate         │ │ Trade execution
  │      ↓             │ │
  │  Report            │─┘
  ├────────────────────┤
  │  주요 메서드:       │
  │  • runBacktest()   │ → IResult<IBacktestResult>
  │  • simulate()      │ → 캔들 반복, 시그널 감지
  │  • calculatePositionSize() │ → fixed_amount, fixed_percent, risk_based
  │  • analyzeResult() │ → 수익률, 샤프비율, MDD, 승률, 손익비
  │  • compareStrategies() │
  └────────────────────┘

  ┌────────────────────┐
  │  OrderExecutorAgent│  apps/hephaitos/src/agents/order-executor-agent.ts
  │  (633 lines)       │
  ├────────────────────┤
  │  워크플로우:        │
  │  Validate          │─┐
  │      ↓             │ │
  │  Submit            │ │ Risk check
  │      ↓             │ │ Order submission
  │  Execute           │ │ Execution monitoring
  │      ↓             │ │
  │  Update            │ │ Position update
  │      ↓             │ │
  │  Monitor (SL/TP)   │─┘
  ├────────────────────┤
  │  주요 메서드:       │
  │  • submitOrder()   │ → IOrderSubmitResult
  │  • closePosition() │ → 슬리피지 포함 청산
  │  • getRiskStatus() │ → IRiskStatus (일일 손실/거래 한도)
  └────────────────────┘

  ┌────────────────────┐
  │  PortfolioSyncAgent│  apps/hephaitos/src/agents/portfolio-sync-agent.ts
  │  (324 lines)       │
  ├────────────────────┤
  │  워크플로우:        │
  │  Connect           │─┐
  │      ↓             │ │ Multi-exchange
  │  Fetch             │ │ Parallel sync
  │      ↓             │ │ Concurrency control
  │  Sync              │─┘
  ├────────────────────┤
  │  주요 메서드:       │
  │  • syncPortfolio() │ → 단일 포트폴리오 (타임아웃)
  │  • syncAllPortfolios() │ → 병렬 동기화
  └────────────────────┘
```

### 2.2 타입 시스템

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    packages/types/src/hephaitos/                         │
└─────────────────────────────────────────────────────────────────────────┘

  strategy.ts
  ├── StrategyType: 'trend_following' | 'mean_reversion' | 'momentum' |
  │                 'breakout' | 'grid' | 'dca' | 'custom'
  ├── Timeframe: '1m' | '5m' | '15m' | '30m' | '1h' | '4h' | '1d' | '1w' | '1M'
  ├── IndicatorType: 'price' | 'sma' | 'ema' | 'rsi' | 'macd' |
  │                  'bollinger' | 'atr' | 'volume' | 'vwap'
  ├── ComparisonOperator: 'gt' | 'lt' | 'gte' | 'lte' | 'eq' |
  │                       'cross_above' | 'cross_below' | 'between'
  └── IStrategy (22 fields): entryConditions, exitConditions,
                             positionSizing, riskManagement

  backtest.ts
  ├── BacktestStatus: 'pending' | 'running' | 'completed' | 'failed' | 'cancelled'
  ├── IBacktestConfig: initialCapital, startDate, endDate, feeRate, slippage
  ├── IPerformanceMetrics (22 metrics):
  │   ├── totalReturn, annualizedReturn, monthlyReturn
  │   ├── sharpeRatio, sortinoRatio, calmarRatio
  │   ├── maxDrawdown, avgDrawdown, maxDrawdownDuration
  │   ├── totalTrades, winRate, profitFactor
  │   ├── avgWin, avgLoss, maxWin, maxLoss
  │   ├── maxConsecutiveWins, maxConsecutiveLosses
  │   ├── avgHoldingPeriod, pnlStdDev, avgTradeReturn, expectancy
  └── DEFAULT_BACKTEST_CONFIG: initialCapital: 10000, feeRate: 0.1%, slippage: 0.05%

  order.ts
  ├── TimeInForce: 'gtc' | 'day' | 'ioc' | 'fok'
  ├── ExecutionMode: 'simulation' | 'paper' | 'live'
  ├── IOrderRequest: symbol, side, type, quantity, price, stopPrice
  ├── IRiskConfig:
  │   ├── maxRiskPerTrade: 2%
  │   ├── maxPositionSize: 20%
  │   ├── maxOpenPositions: 5
  │   ├── dailyLossLimit: 5%
  │   ├── dailyTradeLimit: 10
  │   ├── defaultLeverage: 1, maxLeverage: 3
  │   ├── defaultStopLossPercent: 2%
  │   └── defaultTakeProfitPercent: 4%
  └── DEFAULT_RISK_CONFIG

  exchange.ts
  ├── ExchangeType: 'binance' | 'upbit' | 'bithumb' | 'coinbase' | 'kraken'
  └── EXCHANGE_CONFIGS:
      ├── binance: rateLimit: 1200, features: ['spot', 'futures', 'margin', 'staking']
      ├── upbit: rateLimit: 600, features: ['spot']
      ├── bithumb: rateLimit: 600, features: ['spot']
      ├── coinbase: rateLimit: 600, features: ['spot', 'staking']
      └── kraken: rateLimit: 600, features: ['spot', 'futures', 'margin', 'staking']

  credentials.ts
  ├── CredentialPermission: 'read' | 'trade' | 'withdraw'
  ├── IExchangeCredentials: apiKey, secretKey, passphrase, permissions
  └── IEncryptedCredentials: encryptedData, iv, tag

  asset.ts
  ├── IAsset: symbol, name, amount, price_usd, value_usd, change_24h
  ├── IAssetBalance: asset, free, locked, staking
  └── IAssetBreakdown: symbol, amount, value_usd, percentage

  portfolio.ts
  ├── SyncStatus: 'idle' | 'syncing' | 'success' | 'error'
  ├── IPortfolio: id, user_id, exchange, name, assets, total_value_usd
  ├── IPortfolioSummary: total_value_usd, total_pnl_usd, total_pnl_percent
  ├── IPortfolioSnapshot: id, portfolio_id, asset_breakdown
  └── ISyncResult: success, portfolio_id, synced_at, error

  trade.ts
  ├── OrderSide: 'buy' | 'sell'
  ├── OrderType: 'market' | 'limit' | 'stop' | 'stop_limit'
  ├── OrderStatus: 'pending' | 'open' | 'partial' | 'filled' | 'cancelled' | 'rejected'
  ├── PositionStatus: 'open' | 'closed'
  ├── IOrder: id, symbol, side, type, quantity, price, status
  ├── ITrade: id, orderId, symbol, quantity, price, fee
  ├── IPosition: id, symbol, side, quantity, entryPrice, unrealizedPnL
  ├── IRoundTrip: entryTrade, exitTrade, netPnL, holdingPeriodMs
  └── IOHLCV: timestamp, open, high, low, close, volume
```

### 2.3 유틸리티 (L1 - Molecules)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    packages/utils/src/                                   │
└─────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────┐
  │  backtest-calc.ts (508 lines)  │  백테스트 성과 지표 계산
  ├────────────────────────────┤
  │  수익률 계산:               │
  │  • calculateTotalReturn()  │ → 총 수익률
  │  • calculateAnnualizedReturn()│ → 연환산 (CAGR)
  │  • calculateDailyReturns() │ → 일별 수익률
  │  • calculateMonthlyReturns()│ → 월별 수익률
  ├────────────────────────────┤
  │  리스크 조정 수익률:        │
  │  • calculateSharpeRatio()  │ → 샤프 비율
  │  • calculateSortinoRatio() │ → 소르티노 비율
  │  • calculateCalmarRatio()  │ → 칼마 비율
  ├────────────────────────────┤
  │  낙폭 계산:                 │
  │  • calculateMaxDrawdown()  │ → 최대 낙폭
  │  • calculateAvgDrawdown()  │ → 평균 낙폭
  │  • extractDrawdownRecords()│ → 낙폭 기록
  ├────────────────────────────┤
  │  거래 지표:                 │
  │  • calculateWinRate()      │ → 승률
  │  • calculateProfitFactor() │ → 손익비
  │  • calculateAvgWinLoss()   │ → 평균 이익/손실
  │  • calculateExpectancy()   │ → 기대값
  │  • calculatePerformanceMetrics()│ → 22개 지표 종합
  └────────────────────────────┘

  ┌────────────────────────────┐
  │  signal-detector.ts (451 lines)│  기술적 지표 & 시그널
  ├────────────────────────────┤
  │  지표 계산:                 │
  │  • calculateSMA()          │ → 단순 이동평균
  │  • calculateEMA()          │ → 지수 이동평균
  │  • calculateRSI()          │ → RSI (14)
  │  • calculateMACD()         │ → MACD (12,26,9)
  │  • calculateBollingerBands()│ → 볼린저밴드 (20,2)
  │  • calculateATR()          │ → ATR (14)
  ├────────────────────────────┤
  │  조건 평가:                 │
  │  • evaluateComparison()    │ → 비교 연산자 평가
  │  • evaluateCondition()     │ → 단일 조건 평가
  │  • evaluateConditionGroup()│ → AND/OR 그룹 평가
  ├────────────────────────────┤
  │  시그널 감지:               │
  │  • detectEntrySignal()     │ → 진입 시그널
  │  • detectExitSignal()      │ → 청산 시그널 (SL/TP 포함)
  └────────────────────────────┘

  ┌────────────────────────────┐
  │  validation.ts (195 lines) │  입력 검증
  ├────────────────────────────┤
  │  API 키 검증:               │
  │  • validateApiKey()        │ → 거래소별 키 형식 검증
  │    - Binance: 64자 영숫자   │
  │    - Upbit: UUID 형식       │
  │    - Bithumb: 32자          │
  │    - Coinbase: 16자 key, 88자 secret
  │    - Kraken: 56자 key, 88자 secret
  ├────────────────────────────┤
  │  기타 검증:                 │
  │  • isValidSymbol()         │ → 심볼 형식
  │  • isValidAmount()         │ → 양수 금액
  │  • isValidUUID()           │ → UUID 형식
  │  • validatePortfolioName() │ → 이름 규칙
  └────────────────────────────┘

  ┌────────────────────────────┐
  │  balance.ts (163 lines)    │  잔고 정규화
  ├────────────────────────────┤
  │  • normalizeBalance()      │ → 거래소별 잔고 통일
  │    - Binance → IAsset[]    │
  │    - Upbit → IAsset[]      │
  │    - Bithumb → IAsset[]    │
  │  • calculateTotalValue()   │ → 총 자산 가치
  │  • sortByValue()           │ → 가치순 정렬
  │  • filterDust()            │ → 더스트 제거
  └────────────────────────────┘

  ┌────────────────────────────┐
  │  기타 유틸리티              │
  ├────────────────────────────┤
  │  • currency.ts             │ 통화 변환
  │  • pnl.ts                  │ 손익 계산
  │  • order-calc.ts           │ 주문 계산
  │  • time-series.ts          │ 시계열 처리
  │  • forecast-calc.ts        │ 예측 계산 (FOLIO)
  │  • alarm-eval.ts           │ 알림 평가 (DRYON)
  │  • energy-calc.ts          │ 에너지 계산 (DRYON)
  │  • inventory-calc.ts       │ 재고 계산 (FOLIO)
  │  • geo.ts                  │ 지리 계산
  │  • reward.ts               │ 리워드 계산
  └────────────────────────────┘
```

### 2.4 코어 서비스 (L2 - Cells)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    packages/core/src/services/                           │
└─────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────┐
  │  ExchangeServiceFactory    │  exchange-service.ts (402 lines)
  ├────────────────────────────┤
  │  Interface:                │
  │  • getBalance()            │ → IResult<IAsset[]>
  │  • getTransactions()       │ → IResult<ITransaction[]>
  │  • validateCredentials()   │ → IResult<boolean>
  ├────────────────────────────┤
  │  구현체:                    │
  │  • BinanceService          │ HMAC-SHA256 서명, rate: 1200/min
  │  • UpbitService            │ JWT 토큰, rate: 600/min
  ├────────────────────────────┤
  │  팩토리:                    │
  │  • getService(exchange)    │ → Singleton 패턴
  │  • getSupportedExchanges() │ → ['binance', 'upbit']
  └────────────────────────────┘

  ┌────────────────────────────┐
  │  PriceDataService          │  price-data-service.ts (241 lines)
  ├────────────────────────────┤
  │  Interface:                │
  │  • getHistoricalPrices()   │ → IResult<IPriceData>
  │  • getOHLCV()              │ → IResult<IOHLCV[]>
  │  • resampleTimeframe()     │ → IOHLCV[] (타임프레임 변환)
  ├────────────────────────────┤
  │  구현체:                    │
  │  • InMemoryPriceDataService│ 테스트용 시뮬레이션 데이터
  │    - generateSimulatedData │ 랜덤워크 가격 생성
  └────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                    packages/core/src/repositories/                       │
└─────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────┐
  │  StrategyRepository        │  strategy-repository.ts (241 lines)
  ├────────────────────────────┤
  │  Interface:                │
  │  • save(strategy)          │ → IResult<IStrategy>
  │  • create(input)           │ → IResult<IStrategy>
  │  • getById(id)             │ → IResult<IStrategy | null>
  │  • list(filters)           │ → IResult<IStrategy[]>
  │  • update(id, updates)     │ → IResult<IStrategy>
  │  • delete(id)              │ → IResult<boolean>
  │  • duplicate(id, newName)  │ → IResult<IStrategy>
  ├────────────────────────────┤
  │  구현체:                    │
  │  • InMemoryStrategyRepo    │ Map<string, IStrategy>
  └────────────────────────────┘

  ┌────────────────────────────┐
  │  PortfolioRepository       │  portfolio-repository.ts (322 lines)
  ├────────────────────────────┤
  │  Interface:                │
  │  • save(portfolio)         │ → IResult<IPortfolio>
  │  • getById(id)             │ → IResult<IPortfolio | null>
  │  • getByUserId(userId)     │ → IResult<IPortfolio[]>
  │  • delete(id)              │ → IResult<boolean>
  │  • updateAssets()          │ → IResult<IPortfolio>
  │  • saveSnapshot()          │ → IResult<IPortfolioSnapshot>
  │  • getSnapshots()          │ → IPaginatedResult<IPortfolioSnapshot>
  ├────────────────────────────┤
  │  구현체:                    │
  │  • InMemoryPortfolioRepo   │ Map<string, IPortfolio>
  │  • (TODO) SupabaseRepo     │ Supabase 연동 예정
  └────────────────────────────┘

  ┌────────────────────────────┐
  │  기타 리포지토리            │
  ├────────────────────────────┤
  │  • BacktestResultRepository│ 백테스트 결과 저장
  │  • OrderRepository         │ 주문 이력 저장
  │  • ForecastRepository      │ 예측 데이터 저장
  │  • AlarmRepository         │ 알림 설정 저장
  │  • CompetitorRepository    │ 경쟁사 분석 데이터
  │  • EnergyRepository        │ DRYON 에너지 데이터
  │  • InventoryRepository     │ 재고 관리 데이터
  │  • FeedbackRepository      │ 피드백 저장
  └────────────────────────────┘
```

---

## 3. No-Code 빌더 시스템

### 3.1 빌더 레이아웃

```
┌─────────────────────────────────────────────────────────────────────────┐
│  전략 빌더 레이아웃                                                      │
└─────────────────────────────────────────────────────────────────────────┘

  ┌───────────┬─────────────────────────────────┬─────────────────┐
  │           │                                 │                 │
  │  블록     │         캔버스                   │    설정         │
  │  팔레트   │                                 │    패널         │
  │           │                                 │                 │
  │  170px    │         flex-1                  │    280px        │
  │           │                                 │                 │
  └───────────┴─────────────────────────────────┴─────────────────┘

  ├─────────────────────────────────────────────────────────────────┤
  │  📊 미니 차트 | 실시간 시그널 | 최근 백테스트                  │
  └─────────────────────────────────────────────────────────────────┘
```

### 3.2 블록 인벤토리 (17개)

```typescript
// apps/hephaitos/src/components/builder/block-definitions.ts

BlockType = 'indicator' | 'condition' | 'logic' | 'action' | 'risk'
```

| 카테고리 | 블록 | 아이콘 | 색상 | 파라미터 |
|---------|------|-------|------|---------|
| **지표** (5) | | | #3B82F6 (파랑) | |
| | RSI | 📈 | | period(14), source |
| | MACD | 📊 | | fastPeriod(12), slowPeriod(26), signalPeriod(9) |
| | 볼린저밴드 | 📉 | | period(20), stdDev(2) |
| | 이동평균 | 📏 | | period(20), type(SMA/EMA) |
| | 거래량 | 🔊 | | period(20) |
| **조건** (4) | | | #8B5CF6 (보라) | |
| | 크다 (>) | ⬆️ | | threshold |
| | 작다 (<) | ⬇️ | | threshold |
| | 상향돌파 | ↗️ | | threshold |
| | 하향돌파 | ↘️ | | threshold |
| **논리** (3) | | | #EAB308 (노랑) | |
| | AND | ➕ | | - |
| | OR | ➖ | | - |
| | NOT | ❌ | | - |
| **액션** (3) | | | 초록/빨강 | |
| | 매수 | 🟢 | #22C55E | percentage(10) |
| | 매도 | 🔴 | #EF4444 | percentage(100) |
| | 홀드 | 🟡 | #EAB308 | - |
| **리스크** (2) | | | #F97316 (주황) | |
| | 손절 | 🛡️ | | percentage(-5) |
| | 익절 | 💰 | | percentage(+10) |

### 3.3 빌더 타입 정의

```typescript
// apps/hephaitos/src/components/builder/types.ts

interface BlockPort {
  id: string;
  name: string;
  type: 'number' | 'boolean' | 'signal';
  direction: 'input' | 'output';
}

interface BlockParam {
  id: string;
  name: string;
  type: 'number' | 'select' | 'boolean';
  default: any;
  options?: { label: string; value: any }[];
  min?: number;
  max?: number;
}

interface BlockDefinition {
  id: string;
  type: BlockType;
  category: string;
  label: string;
  description: string;
  icon: string;
  color: string;
  inputs: BlockPort[];
  outputs: BlockPort[];
  params: BlockParam[];
}

interface StrategyNode {
  id: string;
  blockId: string;
  position: { x: number; y: number };
  params: Record<string, any>;
  status: 'idle' | 'running' | 'success' | 'error';
  currentValue?: number | boolean;
}

interface StrategyEdge {
  id: string;
  source: { nodeId: string; portId: string };
  target: { nodeId: string; portId: string };
}

interface CanvasState {
  nodes: StrategyNode[];
  edges: StrategyEdge[];
  zoom: number;
  pan: { x: number; y: number };
  selectedNodeId?: string;
}
```

---

## 4. UX 플로우

### 4.1 사용자 여정

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         사용자 여정 (멘티)                               │
└─────────────────────────────────────────────────────────────────────────┘

  1. 발견 (Discovery)
     YouTube 영상 → 랜딩 페이지 → 멘토 채널
                                    │
  2. 가입 (Onboarding)              ▼
     회원가입 → 역할 선택 → 프로필 설정
                              │
  3. 학습 (Learning)              ▼
     강의 탐색 → 결제 → 수강 → 진도 관리 → Q&A
                                     │
  4. 빌드 (Building)                 ▼
     템플릿 선택 → No-Code 빌더 → 커스터마이징 → 백테스트
                                              │
  5. 운용 (Operating)                         ▼
     계좌 연결 → 에이전트 배포 → 실시간 모니터링 → 성과 리포트
                                                   │
  6. 성장 (Growing)                                ▼
     멘토 피드백 → 전략 개선 → 새 전략 학습 → [반복]
```

### 4.2 협업 빌드 시나리오

| # | 시나리오 | 멘토 참여도 | 대상 멘티 | 설명 |
|---|---------|-----------|---------|------|
| A | 완전 가이드 | 100% | 초급 | 실시간 따라하기, 단계별 검증 |
| B | 부분 가이드 | 50% | 중급 | 막힘 시 호출, 포인트 가이드 |
| C | 리뷰 전용 | 20% | 중급 | 비동기 리뷰, 피드백 제공 |
| D | 자유 빌드 | 0% | 고급 | 완전 자율, 필요시 질문 |
| E | 라이브 코딩 | 80% | 혼합 | YouTube Live 연동, 대규모 |
| F | 그룹 빌드 | 60% | 혼합 | 5-10명 소규모 워크숍 |

**결정 트리**:
```
첫 에이전트? → Yes → 시나리오 A (완전 가이드)
            → No → 막힘? → Yes → 시나리오 B (부분 가이드)
                        → No → 완성? → Yes → 시나리오 C (리뷰)
                                      → No → 계속 빌드
```

### 4.3 계좌 연결 매트릭스

| 구분 | 플랫폼 | 인증 방식 | 권한 | 난이도 |
|-----|-------|---------|------|-------|
| 국내주식 | 한국투자증권 (KIS) | API Key → Token | 읽기, 거래 | 중 |
| 해외주식 | Alpaca | OAuth 2.0 | 읽기, 거래 | 쉬움 |
| 코인 | Binance | API Key + HMAC | 읽기, 거래 | 중 |
| 코인 | 업비트 | API Key + JWT | 읽기, 거래 | 중 |

**BYOK (Bring Your Own Key) 인증 방식**:
- OAuth 2.0 (Alpaca)
- Key Vault (KIS, Binance)
- QR 스캔 (Upbit)

---

## 5. 백테스트 결과 UX

### 5.1 핵심 지표 카드

| 지표 | 설명 | 기준 |
|-----|-----|-----|
| 총수익률 | 투자금 대비 수익 | 벤치마크 대비 비교 |
| 승률 | 수익 거래 비율 | 60% 이상 양호 |
| MDD | 최대 낙폭 | -10% 이내 양호 |
| 샤프지수 | 위험 대비 수익 | 1 이상 양호 |

### 5.2 AI 개선 제안 시스템

```
┌─────────────────────────────────────────────────────────────────────────┐
│  AI 분석 엔진 → 개선 포인트 도출                                          │
└─────────────────────────────────────────────────────────────────────────┘

  1. 손절 타이밍 조정 (중요도: ⭐⭐⭐)
     현재: -5% → 제안: -7%
     이유: -5% 손절 후 +3% 반등 케이스 8건 발견
     예상 효과: 승률 +4%p, 총수익 +2.3%p

  2. 진입 필터 추가 (중요도: ⭐⭐)
     현재: RSI < 30만 체크
     제안: MACD 히스토그램 상승 추가
     예상 효과: 승률 +10%p, 거래 횟수 -30%

  3. 포지션 크기 최적화 (중요도: ⭐)
     현재: 고정 10%
     제안: Kelly Criterion 기반 동적 배분 (8.2%)
```

---

## 6. 안전장치 시스템

### 6.1 자동 정지 조건

| 조건 | 설정 | 동작 |
|-----|------|-----|
| 일일 손실 한도 | 예수금의 2% | 에이전트 자동 중지 |
| 연속 손실 | 5회 | 에이전트 자동 중지 |
| API 오류 | 3회 연속 | 에이전트 자동 중지 |
| 시장 급변 | KOSPI -3% | 신규 매수 중단 |

### 6.2 알림 매트릭스

| 이벤트 | 인앱 | 이메일 | 푸시 | 카카오 |
|-------|------|-------|------|--------|
| 매수/매도 시그널 | ✅ | - | ✅ | ✅ |
| 주문 체결 | ✅ | ✅ | ✅ | - |
| 손실 한도 50% | ✅ | ✅ | ✅ | - |
| 손실 한도 도달 | ✅ | ✅ | ✅ | ✅ |
| 일간/주간 리포트 | ✅ | ✅ | - | - |

---

## 7. 데이터 모델

### 7.1 핵심 테이블

```sql
-- 사용자
users (id, email, role, mentor_status, created_at)

-- 멘토 프로필
mentor_profiles (user_id, channel_name, youtube_url, subscribers,
                 specialties[], revenue_share, tier)

-- 강의
courses (id, mentor_id, title, description, price, status,
         duration_weeks, includes_agent)

-- 수강
enrollments (id, user_id, course_id, status, enrolled_at,
             expires_at, completed_at)

-- 진도
progress (id, enrollment_id, lesson_id, status, progress_percent, completed_at)

-- 에이전트
agents (id, user_id, course_id, name, strategy, backtest_result,
        status, created_at)

-- 협업 세션
collab_sessions (id, mentor_id, session_type, status,
                 max_participants, recording_url)

-- 결제/정산
payments (id, enrollment_id, amount, status, payment_method, paid_at)
payouts (id, mentor_id, gross_amount, platform_fee, net_amount, status)
```

---

## 8. 구현 우선순위

### Phase 1: MVP (4주)
- [x] 멘토/멘티 기본 시스템 설계
- [ ] No-Code 빌더 기본 기능
- [ ] 백테스트 엔진 연동
- [ ] 결제/정산 기본 기능

### Phase 2: 확장 (4주)
- [ ] 협업 빌드 (시나리오 A, B)
- [ ] AI 개선 제안
- [ ] 실계좌 연결 (KIS, Alpaca)
- [ ] 알림 시스템

### Phase 3: 고도화 (4주)
- [ ] 라이브 코딩 세션
- [ ] 그룹 빌드
- [ ] 추가 거래소 연동
- [ ] 성과 리포트 고도화

---

## 9. 파일 인덱스

### 엔진 파일
| 파일 | 위치 | 라인 |
|-----|------|-----|
| BacktestAgent | apps/hephaitos/src/agents/backtest-agent.ts | 645 |
| OrderExecutorAgent | apps/hephaitos/src/agents/order-executor-agent.ts | 633 |
| PortfolioSyncAgent | apps/hephaitos/src/agents/portfolio-sync-agent.ts | 324 |

### 타입 파일 (8개) - L0 Atoms
| 파일 | 위치 |
|-----|------|
| strategy.ts | packages/types/src/hephaitos/ |
| backtest.ts | packages/types/src/hephaitos/ |
| order.ts | packages/types/src/hephaitos/ |
| exchange.ts | packages/types/src/hephaitos/ |
| credentials.ts | packages/types/src/hephaitos/ |
| asset.ts | packages/types/src/hephaitos/ |
| portfolio.ts | packages/types/src/hephaitos/ |
| trade.ts | packages/types/src/hephaitos/ |

### 유틸리티 파일 (14개) - L1 Molecules
| 파일 | 위치 | 라인 | 역할 |
|-----|------|-----|------|
| backtest-calc.ts | packages/utils/src/ | 508 | 22개 성과 지표 계산 |
| signal-detector.ts | packages/utils/src/ | 451 | 기술적 지표 & 시그널 감지 |
| validation.ts | packages/utils/src/ | 195 | 입력 검증 (API 키 등) |
| balance.ts | packages/utils/src/ | 163 | 잔고 정규화 |
| currency.ts | packages/utils/src/ | - | 통화 변환 |
| pnl.ts | packages/utils/src/ | - | 손익 계산 |
| order-calc.ts | packages/utils/src/ | - | 주문 계산 |
| time-series.ts | packages/utils/src/ | - | 시계열 처리 |
| forecast-calc.ts | packages/utils/src/ | - | 예측 계산 (FOLIO) |
| alarm-eval.ts | packages/utils/src/ | - | 알림 평가 (DRYON) |
| energy-calc.ts | packages/utils/src/ | - | 에너지 계산 (DRYON) |
| inventory-calc.ts | packages/utils/src/ | - | 재고 계산 (FOLIO) |
| geo.ts | packages/utils/src/ | - | 지리 계산 |
| reward.ts | packages/utils/src/ | - | 리워드 계산 |

### 서비스 파일 (4개) - L2 Cells
| 파일 | 위치 | 라인 |
|-----|------|-----|
| exchange-service.ts | packages/core/src/services/ | 402 |
| price-data-service.ts | packages/core/src/services/ | 241 |
| sales-data-service.ts | packages/core/src/services/ | - |
| place-crawler-service.ts | packages/core/src/services/ | - |

### 리포지토리 파일 (10개)
| 파일 | 위치 | 라인 |
|-----|------|-----|
| portfolio-repository.ts | packages/core/src/repositories/ | 322 |
| strategy-repository.ts | packages/core/src/repositories/ | 241 |
| backtest-result-repository.ts | packages/core/src/repositories/ | - |
| order-repository.ts | packages/core/src/repositories/ | - |
| forecast-repository.ts | packages/core/src/repositories/ | - |
| alarm-repository.ts | packages/core/src/repositories/ | - |
| competitor-repository.ts | packages/core/src/repositories/ | - |
| energy-repository.ts | packages/core/src/repositories/ | - |
| inventory-repository.ts | packages/core/src/repositories/ | - |
| feedback-repository.ts | packages/core/src/repositories/ | - |

### 빌더 파일
| 파일 | 위치 | 라인 |
|-----|------|-----|
| types.ts | apps/hephaitos/src/components/builder/ | 115 |
| block-definitions.ts | apps/hephaitos/src/components/builder/ | 396 |

### UX 문서
| 문서 | 위치 |
|-----|------|
| B2B2C 전략 | .forge/HEPHAITOS_B2B2C_STRATEGY.md |
| 멘토/멘티 시스템 | .forge/MENTOR_MENTEE_UX_LOGIC.md |
| UX 시뮬레이션 인덱스 | .forge/ux-simulation/00_INDEX.md |
| 협업 빌드 시나리오 | .forge/ux-simulation/01_COLLAB_BUILD_SCENARIOS.md |
| No-Code 빌더 UI | .forge/ux-simulation/02_NOCODE_BUILDER_UI.md |
| 백테스트 결과 UX | .forge/ux-simulation/03_BACKTEST_RESULTS_UX.md |
| 실계좌 연결 UX | .forge/ux-simulation/04_LIVE_TRADING_UX.md |

---

*HEPHAITOS Architecture v1.0.0*
*2025-12-19*
